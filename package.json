{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --filter ./apps/* dev",
    "build": "pnpm --filter ./apps/* build",
    "lint": "pnpm --filter ./apps/* lint"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
